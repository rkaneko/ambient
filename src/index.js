const React = require('react');
const ReactDOM = require('react-dom');

const App = require('./component/App.js');

ReactDOM.render(
  <App />
  , document.getElementsByClassName('main-container')[0]
);
